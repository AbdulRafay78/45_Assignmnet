// function storeCarInfo(manufacturer: string, model: string, ...options: [string, any][]): { [key: string]: any } {
//     const carInfo: { [key: string]: any } = {
//         manufacturer: manufacturer,
//         model: model
//     };

//     options.forEach(([key, value]) => {
//         carInfo[key] = value;
//     });

//     return carInfo;
// }

// const car = storeCarInfo('Toyota', 'Corolla', ['color', 'blue'], ['year', 2022]);

// console.log(car);
